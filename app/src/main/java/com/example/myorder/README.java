package com.example.myorder;

public interface README {
    /*
     * Так как основная идея паттерна MVVM,
     * основана на связывании данных, то
     * стремился реализовать следующим способом:
     * каждый объект живёт своей жизнью, т.е.
     * ничего не знает об объекте, который уровнем
     * выше его. Объекты, которые выше уровнем, могут
     * лишь наблюдать за изменением состояния нижних,
     * и на основе этих изменений принимать решения.
     */
}